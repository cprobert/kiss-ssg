---
branch: review/v2-critical-friend
base: main
status: open
opened: 2026-09-05
---

# Session — 2026-09-05: Independent review of the v2 engine, then implement what survives triage

## Intent (captured at /branch-open)

**Objective:** Give the v2 engine and its gate tooling the independent critical-friend review they never had before 2.0.0 is cut, hand every verified finding to the operator to triage, then implement what they choose on this same branch — plus a design spec, no code, for a watch-mode dependency graph.

**Success criteria:**

- [ ] Every `lib/` module and every `scripts/` module is covered by exactly one review lane (A lifecycle, B watch/replay, C paths, D helpers/contract, E gate tooling).
- [ ] Every finding carries a repro script or a named test that demonstrates it, or is explicitly labelled reasoning-only.
- [ ] Every finding is verified by the orchestrator with a recorded verdict (CONFIRMED / PLAUSIBLE-UNVERIFIED / REJECTED) before the operator sees it.
- [ ] The review report is committed to `planning/reviews/2026-09-05-v2-engine-review.md`.
- [ ] The four open items from the previous branch's retrospective (`_registrations` aliasing, `handlebars-helpers` coverage, docs-site built in dev mode, cutting 2.0.0) appear in the backlog, each with a recommendation.
- [ ] The dependency-graph design spec exists at `planning/specs/2026-09-05-watch-dependency-graph-design.md`, mirrors the v2 spec's sections, states the sync-render invariant, and proposes characterisation tests before any optimisation.
- [ ] Every triaged fix lands with a regression test confirmed to fail on the unfixed code, and `npm run gates` is green after each commit.

**Non-goals / out of scope:** No engine changes during the review phase. No implementation of the dependency graph. No `/branch-close`, no PR, no publish. Skills and prose docs are not code-reviewed. Backfilling historical tags is the operator's task, outside this branch.

**Impact surface:** engine internals — the review touches nothing; the fixes will touch `lib/` and `scripts/` with the public API unchanged unless triage says otherwise, in which case this line is amended.

**Expected shape:** emergent — the findings decide the second half of the branch.

**Delegation convention for this branch** (from `planning/specs/2026-09-02-v2-solid-refactor-design.md` § Model delegation): Fable writes briefs and specs and verifies; Opus reviews and implements; Sonnet does mechanical work. Lifecycle fixes are briefed and reviewed by Fable and implemented by Opus; Fable authors code only where a fix is cross-cutting enough that the brief would be longer than the diff.

### Amendments

<!-- Where adjacent scope drift is absorbed: if the remit legitimately expands
     mid-branch, append a dated note here and stay on the branch — a new branch is
     the operator's call, never spawned on initiative. Good drift gets recorded;
     it is not silent scope creep. -->

- **2026-09-05 — Triage received; implementation phase opens.** Operator's decisions: implement **everything, including P3**; **A-04 and F-01 flip to "failures fail the build"** (`complete()` rejects, the pinning tests are flipped deliberately, llms.txt states the rule); **`folders.root` is deleted** rather than implemented; the watch-mode spec's **step 2** (serial rebuild queue + partial-edit fast path) lands on this branch after step 1, step 3 stays unbuilt pending measurement. Accepted as-is: E3, E7. **Impact surface amended to "public API additions"**: `config.livereloadPort` and `config.devHost` are new keys and `folders.root` is removed, so the close proposes the prerelease bump with the removal called out in CHANGELOG. Implementation success criteria: every triaged row lands with a regression test shown failing on the unfixed code; every `lib/` change ships its AIKB doc, and every consumer-visible change its llms.txt/README/CHANGELOG line, in the same commit; `npm run gates` green after every commit; spec steps 1–2 landed with the replay-equivalence test; the report's Triage column filled. Work plan: `/root/.claude/plans/fable-pickup-from-opus-atomic-dewdrop.md` Part 2 (waves 1–3, one Opus/Sonnet item at a time, Fable reviews every diff).
- **2026-09-05 — Livereload compatibility (W3-9), operator-requested at the livereload steer pulse.** Adjacent to W3-4 (same dev-server seam), absorbed on this branch: the injected livereload snippet derives its host from `location.hostname` at runtime instead of hardcoding `localhost`, so `devHost: '0.0.0.0'` live-reloads on another device; livereload gets `delay: 100` because its watcher has no write-settle and `fs.outputFile` creates a file before filling it; `extraExts` adds svg/webp/avif/ico/woff/woff2 (not json/xml, which would reload on every build's debug siblings and sitemap). No new config key. Impact surface unchanged (engine internals).
- **2026-09-05 — Operator-requested close-out checks, absorbed on this branch.** (1) Examples redesigned with a shared, presentable template (examples are not shipped; no public API change). (2) The kiss-built docs site (`src/` → `docs/`) checked against the branch's changes and updated. (3) A real consumer, `learna-ltd/diploma-msc`, cloned to `/home/user/diploma-msc` (outside this repo) and built with this branch's engine, including dev mode and live reload; any defect found is a finding for triage, not fixed on sight. (3b) A second consumer with a different shape, `learna-ltd/metacarpus` (versioned output, updated each intake), cloned to `/home/user/metacarpus` and put through the same migration, build, dev-mode and live-reload test. (4) A gap analysis of what the generator is missing, as `planning/reviews/2026-09-05-ssg-gap-analysis.md`, informative only. Impact surface unchanged.

## Pulse log

<!-- Appended by /branch-pulse, one dated line per mid-branch checkpoint:
     criteria status + evidence + the continue/adjust/amend/close decision.
     Append-only — the Intent above stays immutable; criteria are ticked only at close. -->

- **2026-09-05** — criteria 1–6 met: every `lib/`/`scripts/` module lane-covered once the in-context addendum F swept `controller-resolver.js` and `logger.js` (report `506e33d`); every row has a repro path or a "reasoning only" label; verdict ledger in the appendix; report committed; four carried-over items each carry a recommendation; spec at `dd64029` mirrors the v2 spec's sections, states the sync-render invariant (marker in the render seam, after critique) and puts characterisation tests first. Criterion 7 not yet — blocked on operator triage (3 P1, 22 P2, 19 P3). Trajectory is three `planning/` files, no engine change, impact surface unchanged, no drift. Decision: continue — hand the backlog to the operator.
- **2026-09-05 (wave 1)** — implementation criteria on track: W1-0…W1-4 landed (`2f7c410`…`9c6cd3e`), every fix with a failing-first test and its flipped characterisation named in the commit; suite 206/206 across 27 files; `npm run gates` green at each commit; spec steps 1–2 landed with the permanent replay-equivalence test (six edits, byte-identical build dirs, zero replays on the four partial/layout/page steps); lane-B repro sweep: eleven scripts, none a silent success. Beyond-brief work absorbed and named: scan() registrations dropped when their view vanishes, re-scan on replay, dedupe against `_registrations`. Impact surface still engine internals (no new config key yet — W3-4 adds them). No drift. Decision: continue to wave 2.
- **2026-09-05 (wave 2)** — W2-1…W2-4 landed (`1775220`…`a83c9a1`) plus W3-7 gates out of order (`7425233`, disjoint files); every item with a failing-first test and its flips named; suite 256/256; lane-A repros `repro-a2-nohandler`/`repro-a2` now exit 0 with one failure report, `repro-cbthrow*` reject, `repro-a3-race`/`repro-a5-await` render the late page; lane-C `c7`, `c1site`, `c8` show fixed or loud behaviour. Beyond-brief work named: the fourth controller path, a depth counter for nested complete(). Impact surface: still no new config key (W3-4 adds them). Decision: continue to wave 3.
- **2026-09-05 (wave 3, close-out)** — every triaged row landed: W3-1…W3-8 (`f49c52c`…`b421ed0`), plus two follow-ups beyond the triage (CHANGELOG order `6e66314`; a watch-test flake root-caused to asserting mid-sweep, fixed by waiting on the rebuild queue, and `_replay()`'s try widened so a replay that throws before re-queuing keeps the last good build and parks unswept paths for the next, `09e2e5f`). Implementation criteria: every row has a failing-first test (flips named in each commit) — met; AIKB/llms/README/CHANGELOG in the same commit — met; gates green: `npm run gates` ✓ test ✓ lint ✓ format (58 files) ✓ pack (33 files) at `09e2e5f`; suite 313/313, `lib/` coverage 94.8% stmts / 92.8% branch (handlebars-helpers 98% branch, from 67%); spec steps 1–2 landed with the permanent replay-equivalence test — met; Triage column filled — met. Smoke: examples 1/4/6 build from `examples/` (sitemap correct), `node docs` exits in 0.6 s with no livereload tag and `docs/` unchanged, a real two-instance dev-server probe survives the livereload clash and works on two ports; repro sweep across lanes A–D: every script shows fixed or loud behaviour (b4 remains the documented controller-purity contract). Impact surface now **public API additions + one removal**: `livereloadPort`, `devHost` added; `folders.root` removed; `globFiles(dir, pattern)` signature changed (shipped only in alpha.1). Surfaced for the operator, not absorbed: `folders.static` is defined and derived but read by nothing (same shape as the removed `root`); the injected livereload snippet still says `localhost`, so `devHost: '0.0.0.0'` serves to another device without live reload. Decision: **ready to close** — `/branch-close` when the operator asks; it will propose the prerelease bump.
- **2026-09-05 (steer: livereload compatibility)** — operator asked whether anything is still needed. Evidence: `livereload` 0.10.3 is current (published 2025-08); its own README snippet derives the host from `location.host`, kiss's injected snippet hardcodes `localhost` (`lib/kiss-page.js:105`), so `devHost: '0.0.0.0'` serves to another device without live reload — the one real gap, already surfaced at close-out; livereload's watcher (`livereload.js:174`) has no write-settle and refreshes on `add` with `delay` 0, so a browser can fetch a page mid-write — small window, README names `delay` as the remedy; default watched extensions omit `svg`/`webp`/`avif`/`woff2`, so editing those assets does not reload (`extraExts`). Criteria unchanged from the close-out pulse (all met; suite 313/313; tree clean). These three are adjacent to W3-4 (same subsystem), not creep; recommended as one small item if the operator wants it — not absorbed on initiative. Decision: **ready to close** stands; awaiting the operator's call on the livereload item.
- **2026-09-05 (W3-9 landed)** — livereload compatibility at `1ce9de9`: snippet host resolved at runtime (verified in built output of examples 2 and 4, which have a `</body>`; example 1 has none and never carried a snippet), `delay: 100`, `extraExts` for svg/webp/avif/ico/woff/woff2; failing-first tests for all three, real-livereload behavioural case (svg refreshes, json does not); suite 315/315 — correction: the new svg-refresh test failed once in my second full-suite run and was root-caused (the test wrote before livereload's chokidar initial scan had finished, so with ignoreInitial the file never emitted add) and fixed test-side at `588d1c5`, no sleep added, verified 3× full suite + 6× under load; two-port probe: each site's pages carry their own port, no literal localhost. Criteria all still met; the Amendment covers the drift. Left as-is and named: README's `env`-helper example shows a hand-written `localhost:35729` tag (illustrative, not engine output). Decision: **ready to close** — `/branch-close` when the operator asks.

---

<!-- /branch-close → /retrospective fills the Reflection below and flips status: closed -->
