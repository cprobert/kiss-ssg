---
branch: chore/2.0.0-release
base: main
status: open
opened: 2026-09-09
---

# Session — 2026-09-09: Cut kiss-ssg 2.0.0 stable, drop the beta tag

## Intent (captured at /branch-open)

**Objective:** Declare the v2 engine stable — bump `2.0.0-beta.2` to `2.0.0`, sync the
public-facing docs, and publish to npm with the `latest` dist-tag so a bare
`npm install kiss-ssg` gets v2 by default.

**Success criteria:**

- [ ] All four internal consumer sites confirmed compatible with the code on `main`
      (done immediately before this branch opened: diploma-msc, learna-kiss,
      student-handbooks, a1k9training all build clean against current `main`)
- [ ] `package.json` version is `2.0.0`, no prerelease tag
- [ ] `CHANGELOG.md` gets a `2.0.0` entry
- [ ] `/docs-sweep` run — `llms.txt`, `README.md` and `AIKB/` current with `main`
- [ ] `/corpse-collector` run, findings triaged
- [ ] `/secrets-scan` run clean
- [ ] `/test-coverage-check --gate` passes
- [ ] `npm run gates` green
- [ ] `/retrospective` written
- [ ] PR opened against `main`
- [ ] `npm publish` executed, `latest` dist-tag moved from `1.4.4` to `2.0.0`

**Non-goals / out of scope:**

- No further `lib/` engine changes on this branch — the code being declared stable is
  already on `main` and was verified as-is against the four consumer sites.
- Not modifying the four consumer sites further, and not merging/pushing their own
  `kiss-v2` branches to their own `main`/`master` — that adoption timing is each site's
  own call, separate from this engine release.

**Impact surface:** public API — the branch's substance is declaring the already-built
v2 surface stable and moving the npm `latest` tag onto it; obliges `llms.txt` +
`README.md` currency and a real version bump, even though no `lib/` code changes here.

**Expected shape:** planned — the destination (2.0.0, `latest`, published) is fixed; the
route is the standard close ritual.

### Amendments

<!-- Where adjacent scope drift is absorbed: if the remit legitimately expands
     mid-branch, append a dated note here and stay on the branch — a new branch is
     the operator's call, never spawned on initiative. Good drift gets recorded;
     it is not silent scope creep. -->

## Pulse log

<!-- Appended by /branch-pulse, one dated line per mid-branch checkpoint:
     criteria status + evidence + the continue/adjust/amend/close decision.
     Append-only — the Intent above stays immutable; criteria are ticked only at close. -->

---

<!-- /branch-close → /retrospective fills the Reflection below and flips status: closed -->
