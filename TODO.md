# Todo

Review findings from 2026-09-21. Reproduced in temporary fixtures; the existing
test suite passed with normal process access. These are outstanding suggestions,
not completed fixes.

| Priority | Finding                                                                                                                              | Suggested improvement                                                                                                                                                                                                 | Status                 |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- |
| High     | Build cleanup can delete configured source files: the guard checks `folders.src` but misses independently configured source folders. | Validate the build destination against every configured source location before any filesystem mutation, including equivalent paths and filesystem aliases. Reject `folders.src` resolving to the project root (`./`). | Implemented and tested |
| High     | Staging cleanup deletes staging folders belonging to other live processes.                                                           | Check ownership and liveness before removing staging folders; preserve recovery backups conservatively.                                                                                                               | Open                   |
| High     | Asset hashing renames JavaScript dependencies without rewriting their import URLs; the build can report success with broken modules. | Define and enforce the supported hashing boundary: standalone bundles, dependency-aware rewriting, or preserved dependency URLs.                                                                                      | Open                   |
| Medium   | Remote-model timeouts end when headers arrive, leaving a stalled response body unbounded.                                            | Keep timeout and cancellation active through response-body consumption.                                                                                                                                               | Open                   |
| Medium   | Development watchers miss asset additions/deletions and intentional empty source-file saves, leaving previews stale.                 | Handle asset additions and deletions, reconcile removed output, and debounce empty saves without permanently ignoring them.                                                                                           | Implemented and tested |
| Medium   | Explicitly requested auxiliary output can fail while the build still reports `ok: true`.                                             | Distinguish output that was not requested from output that failed; fail explicitly requested output or expose structured warnings and a strict policy.                                                                | Open                   |
