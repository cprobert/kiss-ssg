### Sourcing note

Both Ethiopian lots come through the same importer, on the same ship. When one lands late, so does the other.

_A Markdown partial: kiss renders `.md` partials to HTML as it registers them, so they arrive already compiled._
