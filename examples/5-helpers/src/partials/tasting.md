### From a Markdown partial

Every bag leaves the arch with a card like this one. The card is a `.md` file in the partials folder; kiss renders it to HTML when it registers it, so the page just asks for it by name.
