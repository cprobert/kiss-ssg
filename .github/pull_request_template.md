## Summary

<!-- 1-3 bullets. If the branch was opened with /branch-open, seed this from the
     Objective in planning/sessions/<date>-<slug>.md. -->

## What's deferred

<!-- Delete this section if nothing is deferred. -->

## Test plan

- [ ] `npm run gates` passes (test, lint, format, pack)

<!-- Then the success criteria captured at /branch-open, each as a checkbox. -->
