export function registerPartialsFrom(hbs: any, folder: any, ext: any, { markdown, logger }: {
    markdown: any;
    logger: any;
}, { compiled }?: {
    compiled?: boolean;
}): string[];
export function registerPartials(hbs: any, config: any, deps: any, previous?: any[]): string[];
