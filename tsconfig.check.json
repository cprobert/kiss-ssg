{
  "//": "Type CHECKING only — never emits. `tsconfig.types.json` is the one that emits types/. Kept separate because the emit has to stay tolerant (checkJs off) to produce declarations for code this file is allowed to complain about.",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "target": "ES2022",
    "strict": false,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["lib/**/*.js", "scripts/**/*.mjs", "bin/**/*.js"]
}
