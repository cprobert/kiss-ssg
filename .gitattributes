# LF in the repo and in every working tree, whatever a clone's core.autocrlf
# says. The tests hash and byte-compare files on disk — the example records
# embed their own build script, and test/aikb.test.js searches llms.txt for
# exact line breaks — so a CRLF checkout fails the gates with no code change.
* text=auto eol=lf
